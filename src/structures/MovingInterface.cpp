//
// Created by lessmeaning on 15.06.2020.
//

#include <bits/stdc++.h>

#include "../../includes/structures/MovingInterface.h"

int MovingInterface::move(int from, int to, long long dst) {
    assert(0);
    return -1;
}
long long MovingInterface::get_dist(int u, int v) {
    assert(0);
    return -1;
}
int MovingInterface::lca(int u, int v) {
    assert(0);
    return -1;
}

MovingInterface::~MovingInterface() {

}